'use strict';

/**
 * candidate-role controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::candidate-role.candidate-role');
